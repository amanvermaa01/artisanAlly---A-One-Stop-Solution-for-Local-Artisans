{
  "name": "artisan-ally-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd artisanAlly-frontend && npm install && npm run build",
    "postinstall": "cd artisanAlly-backend && npm install"
  },
  "engines": {
    "node": ">=18"
  }
}
